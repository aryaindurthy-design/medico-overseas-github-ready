# GitHub Submission Notes

This repository intentionally excludes generated dependencies and secrets.

- Install backend dependencies with `cd backend && npm install`.
- Configure local secrets using `backend/.env.example` as a template.
- `backend/node_modules/` is excluded because it is generated by npm.
- `backend/callbacks.db` is excluded from Git because it is runtime data.
